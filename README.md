# 🚀 DeepSeek Harness 一键启动器（Windows）

一个**完全不用懂代码**也能用的工具：双击一下，就能帮你在电脑上把 DeepSeek Harness（一个在浏览器里运行的 AI 工具）启动起来。

> 💡 看不懂没关系，下面全部用大白话讲。

---

## 这个工具是干什么的？

DeepSeek Harness 是一个**需要先在电脑后台"运行"才能在浏览器里打开的软件**（就像一个电台，得先开机发射信号，你才能收到频道）。

这个仓库里有 **2 个小脚本**，能帮你完成"开机发射信号"这一步。你不用敲任何命令，**双击就行**。

- ✅ **适合完全没碰过代码的新手**
- ✅ 不需要学任何命令
- ✅ 全程点点鼠标

---

## 📁 两个脚本，分别能干嘛？

| 脚本文件 | 大白话解释 | 什么时候用它 |
| -------- | ---------- | ------------ |
| **`OpenDeepSeek.vbs`** | 在**后台**默默把服务启动起来。它**不会**自动弹出窗口，你自己再用浏览器打开网址就行。 | 你希望服务一直在后台跑着，想什么时候打开网页就什么时候打开 |
| **`Start DeepSeek Harness.vbs`** | 启动服务，**等它完全准备好后，自动帮你弹出浏览器页面**。 | 你只想一劳永逸——双击一下，一切都搞定 |

简单记：

```
OpenDeepSeek.vbs        =  只开"后台"，网页我自己开
Start DeepSeek Harness  =  开"后台" + 自动开网页（推荐新手选这个）
```

---

## 🎯 我需要装什么？（前提条件）

只用确认一件事：**电脑上已经装好一个叫 Node.js 的东西**（很多工具都靠它运转）。

> 怎么确认装没装？打开"开始菜单"，输入 `cmd` 回车，在弹出的黑窗口里输入：
> ```
> dsh web
> ```
> - 如果没报"找不到命令"，说明可以用了 ✅
> - 如果报"不是内部或外部命令"，说明还缺 Node.js，装好再来。

---

## ▶️ 怎么用？（超简单三步）

1. **把两个脚本文件下载到你的电脑**（随便放哪个文件夹都行，比如 `D:\dsh`）。
2. **用记事本打开脚本，改一个地方**（见下面"⚠️ 唯一需要改的地方"）。
3. **双击脚本**，搞定。

---

## ⚠️ 唯一可能需要改的地方

每个脚本的**顶部第一行**都有一句：

```
WorkPath = "D:\dsh"
```

它的作用是告诉脚本"我的工作文件夹在哪"。

- 如果你把脚本放在 `D:\dsh` 这个文件夹 → **什么都不用改**，直接双击就用。
- 如果你放在**别的地方** → 把 `"D:\dsh"` 改成你放脚本的那个文件夹路径即可。

> 📌 就算不改一般也能启动成功，除非程序提示找不到路径时再回来改。

---

## 🆚 两个脚本的启动体验对比

| | `OpenDeepSeek.vbs` | `Start DeepSeek Harness.vbs` |
| --- | --- | --- |
| 后台启动服务 | ✅ | ✅ |
| 自动等待服务就绪 | ❌（不等，后台转） | ✅（每 0.2 秒检查一次） |
| 自动弹出浏览器 | ❌ 不弹，自己开 | ✅ 自动弹 |
| 适合人群 | 熟悉一点的用户 | **完全新手（推荐）** |
| 会不会白屏等待 | 由自己刷新 | 不会，就绪才弹 |

> `Start DeepSeek Harness.vbs` 用的是"轮询等待"：每 0.2 秒看一次服务准备好没，好了立刻弹浏览器，**快且不白屏**。

---

## ❓ 常见问题（FAQ）

**Q: 双击后浏览器没弹出来？**
A: 先确认服务真的启动了——手动打开浏览器，输入 `http://127.0.0.1:3080` 看看能不能访问。能访问说明脚本没问题，只是没弹出来。

**Q: 双击后闪了一下就没了？**
A: 这**是正常现象**。脚本本来就是在后台干活，窗口一闪而过就是在后台启动服务的信号。

**Q: 我根本不知道 Node.js 是什么，能装吗？**
A: 能，搜"Node.js 中文官网"下载安装即可（安装时一路点"下一步"就行）。装完再回来看本文件。

---

## 🛠 想自己微调？（进阶）

以下都写在脚本顶部，用记事本打开就能改：

- `WorkPath`：工作文件夹路径（见上文）。
- `ENABLE_AUTO_START = False`：改成 `True` 可开机自动启动（建议先别改，等熟悉了再说）。

---

## 📄 作者

由 `awsl0205-svg` 整理分享，仅供学习与方便使用。

---

**如果觉得好用，欢迎点个 ⭐ Star 支持一下！**
